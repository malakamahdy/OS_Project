{
  "name": "containershield-agent",
  "version": "1.0.0",
  "description": "Monitoring agent for ContainerShield — reports container metrics to central backend",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dockerode": "^4.0.2"
  }
}
